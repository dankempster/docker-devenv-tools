version: "2"

services:
  adminer:
    image: clue/adminer
    environment:
      - VIRTUAL_HOST=adminer.dev
    networks:
      - tools
      - proxy

networks:
  tools:
  proxy:
    external:
      name: reverseproxy_net
